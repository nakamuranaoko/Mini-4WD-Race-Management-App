<div class="container mx-auto py-4">
  <h1 class="text-2xl font-bold text-center mb-6">あなたのイベント結果一覧</h1>

  <!-- 共通検索フォーム呼び出し -->
    <%= render 'search_form', search_path: user_index_events_path, show_user_name: false %>

  <!-- 共通テンプレートの呼び出し -->
  <%= render 'event_list', events: @events, hide_user_name: true %>
</div>